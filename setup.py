from setuptools import setup, find_packages

setup(
    name="Lunch",
    description="Have a Sodexo menu at your terminal.",
    version="0.1",
    packages=find_packages(),
    scripts=['lunch']
)
